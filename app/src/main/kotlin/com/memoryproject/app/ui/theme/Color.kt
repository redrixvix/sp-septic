package com.memoryproject.app.ui.theme

import androidx.compose.ui.graphics.Color

// Warm editorial palette — premium, emotionally resonant
val Cornsilk = Color(0xFFFEFAE0)        // Primary background — warm cream
val Beige = Color(0xFFE9EDC9)            // Subtle secondary surfaces
val TeaGreen = Color(0xFFCCD5AE)         // Accents, tags, secondary actions
val TeaGreenDark = Color(0xFF6A7A5A)       // Darker variant for storage text
val Bronze = Color(0xFFD4A373)           // Primary brand color — warm gold
val BronzeLight = Color(0xFFE8C19A)       // Hover/pressed state
val BronzeDark = Color(0xFFB8815A)       // Active/selected state
val Papaya = Color(0xFFFAEDCD)           // Card backgrounds, elevated surfaces
val WarmWhite = Color(0xFFFFFCF5)        // Pure card surfaces
val Charcoal = Color(0xFF3D3D3D)          // Primary text — soft but readable
val CharcoalLight = Color(0xFF5C5C5C)     // Secondary text
val CharcoalMuted = Color(0xFF5C5C5C)     // Muted text — accessible 4.5:1+ on all light backgrounds
val Border = Color(0xFFD4D0B8)            // Subtle warm borders
val Divider = Color(0xFFE8E4D0)           // Soft dividers
val ErrorRed = Color(0xFFB84040)          // Clear, warm error color
val SuccessGreen = Color(0xFF5A8A5A)     // Soft success
val SuccessGreenDark = Color(0xFF4A7A4A)   // Darker forest green — shared/active states in light theme

// Section headers & soft card backgrounds
val SectionHeaderBackground = Color(0xFFFAF5E6)  // Warm cream for settings/cards headers

// Memory card accent colors — rotate through for visual interest
val CardAccentBronze = Color(0xFFD4A373)
val CardAccentTea = Color(0xFF7A9A6A)
val CardAccentPapaya = Color(0xFFE8C19A)

// Dark theme colors — warm, cozy, premium
val DarkBackground = Color(0xFF1A1814)     // Deep warm black
val DarkSurface = Color(0xFF2A2520)         // Elevated surface
val DarkSurfaceVariant = Color(0xFF3A3530)  // Cards and containers
val DarkOnSurface = Color(0xFFF5F0E8)      // Warm white text on dark
val DarkOnSurfaceVariant = Color(0xFFB8B0A0) // Muted text on dark
val DarkBorder = Color(0xFF4A4540)          // Dark borders
val DarkDivider = Color(0xFF3A3530)        // Dark dividers
val DarkBronze = Color(0xFFE8C19A)          // Lighter bronze for dark theme
val DarkBronzeLight = Color(0xFFF0D4B0)     // Hover for dark
val DarkError = Color(0xFFD87070)          // Softer red for dark mode
val DarkSuccess = Color(0xFF7AAA7A)        // Softer green for dark mode